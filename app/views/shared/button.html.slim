= form_tag nil, method: :post
  input type='hidden' name='back_to' value=(params[:back_to] || request.referer)
  input type='hidden' name='turn' value=params[:turn]
  noscript: button Submit

javascript:
  document.forms[0].submit()
  document.write('Refreshing...')
