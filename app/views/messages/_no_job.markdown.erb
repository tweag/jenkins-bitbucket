# No job on Jenkins

As far as I can tell, there isn't a job on Jenkins for story number <%= message[:pull_request].identifier %>.

To fix this, run a job on Jenkins that has "<%= message[:pull_request].identifier %>" in the name.
