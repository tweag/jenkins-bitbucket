# No job on Jenkins

As far as I can tell, there isn't a job on Jenkins for branch `<%= message[:pull_request].identifier %>`.

To fix this, run a job on Jenkins for branch `<%= message[:pull_request].identifier %>`.
