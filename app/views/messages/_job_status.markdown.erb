# <%= status_image(message[:job]) %> <%= status(message[:job]) %> on Jenkins

[<%= message[:job].name %> on Jenkins](<%= message[:job].url %>)

<%= render 'messages/checklist.markdown', message: message -%>

###
###
